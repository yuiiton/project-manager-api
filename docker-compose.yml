services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gerenciador-projetos-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      PYTHONUNBUFFERED: "1"
    restart: unless-stopped
